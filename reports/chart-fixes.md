# 채보 수정 인계 — 2026-09-09

범위: 채보 데이터와 편집용 JSON. 음원, 엔진, 기존 기록/리플레이 데이터는 변경하지 않았다.

- Dear END: 시작 0.368초 노트를 실제 신호 시작 이후인 0.813초로 옮겼다. 0.786초의 기존 선행 노트는 합쳤다. 두 노트를 유지하면 새로 27ms 연타가 생기기 때문이다. -50dB 검출 시작은 0.812086초이며 10ms 단위 신호 확인에서도 0.81초 구간에 뚜렷한 어택이 있다. 전곡 청취로 음악적 스냅을 확정한 것은 아니다.
- Dear END: 211.472, 212.025, 212.393(drag), 213.129초의 끝 저레벨 구간 4노트를 삭제했다. END는 731→726노트이며 마지막 노트는 211.166초다.
- 나머지 drag 26개는 위치·시각·지속시간을 보존한 hold로 변환했다. WWSL END 3개, Dear END 23개다. 기존 drag 27개 중 1개는 위 끝부분 삭제에 포함된다. 경로 데이터가 없으므로 기존 홀드 엔진으로 지속시간을 표현하는 선택을 했다. 실제 경로 추적 기능을 추가한 것은 아니다.
- 기존 홀드는 60% 이상 유지하면 tail perfect, 25% 이상이면 good이다. 이 판정은 유지했다. 변환한 노트는 헤드뿐 아니라 테일도 점수에 포함되므로 이전 채보와 점수/플레이 감각이 달라진다. Dear END의 인덱스와 노트 수 변경으로 기존 인덱스 기반 리플레이는 호환되지 않을 수 있다.
- source/charts의 배열 JSON 10개와 manifest를 재추출했다. 원본 metadata와 각 노트를 조합하면 songs.js와 데이터가 정확히 일치한다. 전체 4,333→4,328노트.

검증: 변경 전/후 객체 비교로 지정된 변경 외 차이가 없음을 확인했다. 전체 노트의 유한수·좌표·타입·지속시간·정렬·곡 길이 검사를 통과했다. 실제 buildObjects 함수를 Node VM에서 10채보 모두 실행하고 홀드 테일이 점수 분모에 반영됨을 확인했다. 편집용 JSON의 완전 복원 비교와 음원 SHA-256 검증을 통과했다. 브라우저 실제 플레이 검증은 수행하지 않았다.

Claude 후속 작업: 음원 클리핑/음량 조정과 offset 코드·문서 정리. 임베디드 음원 변경 후 `python3 scripts/sync-charts.py --export`로 manifest의 해시를 갱신한다. 이번 변경은 음원 바이너리를 포함하지 않는다. `audio-chart-review.md` 및 `audio-chart-audit.json`은 수정 전 검토 기록이다.
